/* generated by Svelte v3.46.2 */
import {
	add_attribute,
	compute_rest_props,
	create_ssr_component
} from "svelte/internal";

const Component = create_ssr_component(($$result, $$props, $$bindings, slots) => {
	let $$restProps = compute_rest_props($$props, ["src"]);
	let { src } = $$props;

	// TODO: Consider error handling for required stuff
	// if (!name) throw new Error('No name for component');
	// try {
	// if (!name) throw new Error(`\x1b[31mCayo Component: 'name' prop is required and can't be an empty string`);
	// } catch (err) {
	//   new Error(err);
	// }
	// console.log(Component);
	const json = JSON.stringify($$restProps);

	if ($$props.src === void 0 && $$bindings.src && src !== void 0) $$bindings.src(src);
	return `<div${add_attribute("data-cayo-src", src ? `${src}` : '', 0)}${add_attribute("data-cayo-props", json, 0)}>${slots.default ? slots.default({}) : ``}</div>`;
});

export default Component;